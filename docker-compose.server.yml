services:
  cooplyst:
    image: ghcr.io/${GHCR_OWNER}/cooplyst:v0.1.0
    ports:
      - "3000:80"
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - NODE_ENV=production
    volumes:
      - cooplyst_data:/app/data
    restart: unless-stopped

volumes:
  cooplyst_data:
