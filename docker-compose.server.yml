services:
  cooplyst:
    image: ghcr.io/vigno04/cooplyst:latest
    ports:
      - "3000:80"
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - NODE_ENV=production
    volumes:
      - cooplyst_data:/app/data
    restart: unless-stopped

volumes:
  cooplyst_data:
